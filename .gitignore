.DS_Store
phpcs.xml
phpunit.xml
Thumbs.db
wp-cli.local.yml
node_modules/
.claude/
*.sql
*.tar.gz
*.zip

# vendor/ IS committed (Parsedown ships with the plugin so users don't need composer install)
!vendor/

# dist/ IS committed (built Vue bundle ships with the plugin so users don't need npm install)
!dist/
